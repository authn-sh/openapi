parameters:
  - name: sign_in_id
    in: path
    required: true
    schema: { $ref: ../../components/schemas/Id.yaml }
  - name: challenge_id
    in: path
    required: true
    schema: { $ref: ../../components/schemas/Id.yaml }

get:
  operationId: getSignInChallenge
  summary: Get a challenge on a sign-in
  description: |
    Fetch the focused `Challenge` resource.

    Unlike `getSignIn`, this endpoint returns the unwrapped `Challenge`
    — no envelope, no Client refresh.
  tags: [Sign-Ins]
  security:
    - client_cookie: []
    - dev_browser_jwt: []
  responses:
    "200":
      description: The current state of the challenge.
      content:
        application/json:
          schema: { $ref: ../../components/schemas/Challenge.yaml }
    "401": { $ref: ../../components/responses/Unauthorized.yaml }
    "404": { $ref: ../../components/responses/NotFound.yaml }
    "429": { $ref: ../../components/responses/TooManyRequests.yaml }
