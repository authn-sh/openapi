type: object
description: |
  Body for `PATCH /v1/organizations/{org_id}/memberships/{id}`. v0.2
  supports changing the role; metadata edits are fire-and-forget.
additionalProperties: false
properties:
  role:
    type: string
    description: New role `key`.
  public_metadata:
    $ref: ./Metadata.yaml
  private_metadata:
    $ref: ./Metadata.yaml
